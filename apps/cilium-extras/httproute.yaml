apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble
  namespace: kube-system
spec:
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - "hubble.talos.froystein.jp"
  rules:
    - backendRefs:
        - name: hubble-ui
          port: 80
