apiVersion: v1
kind: Service
metadata:
  name: devbox-ssh
  namespace: devbox
  annotations:
    metallb.io/loadBalancerIPs: 192.168.1.51
spec:
  type: LoadBalancer
  selector:
    vm.kubevirt.io/name: devbox
  ports:
    - name: ssh
      port: 22
      targetPort: 22
      protocol: TCP
