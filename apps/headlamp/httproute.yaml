apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headlamp
  namespace: headlamp
spec:
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - "headlamp.talos.froystein.jp"
  rules:
    - backendRefs:
        - name: headlamp
          port: 80
