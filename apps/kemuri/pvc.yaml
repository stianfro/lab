apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kemuri-data
  namespace: kemuri
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
