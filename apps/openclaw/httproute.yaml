apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: openclaw
  namespace: openclaw
spec:
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
  hostnames:
    - "openclaw.talos.froystein.jp"
  rules:
    - backendRefs:
        - name: openclaw
          port: 80
