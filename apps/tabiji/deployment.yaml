apiVersion: apps/v1
kind: Deployment
metadata:
  name: tabiji
  namespace: tabiji
  labels:
    app.kubernetes.io/name: tabiji
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: tabiji
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tabiji
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: web
          image: ghcr.io/stianfro/tabiji:sha-a2229a5@sha256:2c19b80072e37af1e2e4289d0a531d7791cfd30b082511767cc1d424a023e18c
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: cache
              mountPath: /var/cache/nginx
            - name: run
              mountPath: /var/run
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: cache
          emptyDir: {}
        - name: run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
